/**
 * Initial placeholder values displayed when the user first opens the create form.
 */
export const DEFAULT_RECORD_VALUE = {
  recordName: `Blood report`,
  recordDate: new Date(),
  documentUrl: null,
};