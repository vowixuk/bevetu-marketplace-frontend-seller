
/**
 * The basic interface of every payload.
 */
export interface IBasicPayload {
  accessToken?: string;
  userId: string;
}

/**
 * A marker type indicating that "I will add a more detailed type later"
 */
export type TODO = any;
